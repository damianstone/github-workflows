name: Output information
on: workflow_dispatch
jobs:
  info:
    runs-on: ubuntu-latest
    steps:
      - name: Output GitHub context
        # this syntaxt is called "expression"
        run: echo "${{ toJSON(github) }}" # access to metadata available from github